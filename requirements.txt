# Core scientific Python
numpy>=1.24
scipy>=1.10
pandas>=2.0
matplotlib>=3.7

# Deep learning
torch>=2.0
torchvision

# Graph neural networks
torch_geometric
torch_scatter
torch_sparse
torch_cluster

# MLIP backends (ALCHEMI BMD NIM / BCS NIM)
mace-torch             # MACE-MPA-0, MACE-OFF — BMD NIM + BCS NIM
aimnet2calc            # AIMNet2, AIMNet2-NSE, AIMNet2-CPCM — BCS NIM
# tensornet            # TensorNet-MatPES — BMD NIM (install separately)
# deepmd-kit           # DeepMD-kit GPU build (install via conda)

# Chemistry / materials
ase>=3.22
pymatgen>=2024.1
rdkit                  # conformer generation (BCS NIM step 1)
mp-api                 # Materials Project data access

# Machine learning utilities
scikit-learn>=1.3
tqdm

# Analysis
# autopsy              # custom post-processing (private; pip install from GitHub)
AtomicAI               # custom MLFF + analysis tools (pip install AtomicAI)

# Visualization
plotly
py3Dmol

# HPC / parallelism
mpi4py                 # MPI-parallel descriptor calculation (AtomicAI)

# Data formats
pyarrow                # parquet (Materials Project dataset)
h5py                   # HDF5 trajectory storage

# Drug delivery extras
biopython              # sequence / PDB handling for ADC payloads
